# Sidebar Navigation Module - Flow

## Toggle Flow

```
User clicks toggle button
    ↓
Update isCollapsed state
    ↓
Save to localStorage (if configured)
    ↓
Re-render with new state
```

## Navigation Flow

```
User clicks navigation item
    ↓
Navigate to route
    ↓
Update active state
    ↓
Highlight active item
```
